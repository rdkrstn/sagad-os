// Deprecated: replaced by model-providers-panel.tsx (multi-provider surface).
// Re-export kept so any lingering imports resolve until callers are fully migrated.
export {
  ModelProvidersPanel as ModelGatewayPanel,
  type ModelProvidersView as ModelGatewayView,
} from "./model-providers-panel";
